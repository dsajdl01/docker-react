sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t dsajdl/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dsajdl/docker-react npm run test
